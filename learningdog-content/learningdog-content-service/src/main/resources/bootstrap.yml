spring:
  application:
    name: content-service
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        namespace: dev
        group: learningdog-project
      config:
        namespace: dev
        group: learningdog-project
        file-extension: yaml
        refresh-enabled: true

  profiles:
    active: dev